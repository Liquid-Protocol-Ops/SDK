# Contributing

## Setup
1. Clone: `git clone https://github.com/Liquid-Protocol-Ops/SDK.git`
2. Install: `npm install`
3. Build: `npm run build`
4. Test: `npm test`

## PR Requirements
- Tests pass (`npm test`)
- Types check (`npx tsc --noEmit`)
- Build succeeds (`npm run build`)
