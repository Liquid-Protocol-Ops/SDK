name: "Setup action"
description: "Setup repository and install deps"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 22
    - name: Install deps
      shell: bash
      run: npm ci
